/** These breakpoints are the maximum px that each breakpoint is defined at. */
const breakpoints = {
  values: {
    xs: 359, // < 360px
    sm: 767, // < 768px
    md: 1023, // < 1024px
    lg: 1439, // < 1440px
    xl: 1919, // < 1920px
  },
};

export default breakpoints;
